import React from 'react';

const AuthLayout = () => {
    return (
        <main className={"flex min-h-screen bg-amber-400 flex items-center justify-center"}>
            <div className={"w-full max-w-[min(400px,95vw)] bg-white min-h-[400px]"}>

            </div>
        </main>
    );
};

export default AuthLayout;